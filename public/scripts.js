// public/scripts.js
document.addEventListener('DOMContentLoaded', () => {
    // Check for fullscreen support
    if (!document.fullscreenEnabled) {
      alert('Your browser does not support fullscreen mode, which is required for the test.');
    }
  });